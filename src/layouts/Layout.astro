---
import { ClientRouter } from "astro:transitions";
import SEO from "../components/SEO.astro";
import "../styles/global.css";

interface Props {
	title: string;
	description?: string;
	image?: string;
	canonical?: string;
	structuredData?: object;
}

const { title, description = "Asaa Soluciones: Explora los últimos gadgets tecnológicos y productos adorables del Bazar Kawaii. ¡Encuentra lo que más te guste!", image, canonical, structuredData } = Astro.props;
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/img/favicon.png" />
		<meta name="generator" content={Astro.generator} />

		<!-- SEO Component -->
		<SEO title={title} description={description} image={image} canonical={canonical} structuredData={structuredData} />

		<!-- Additional Meta Tags -->
		<meta name="keywords" content="tecnología, gadgets, bazar, kawaii, productos adorables, tienda online, gadgets únicos, Ecuador" />
		<meta name="author" content="Asaa Soluciones" />
		<meta name="robots" content="index, follow" />

		<ClientRouter />
	</head>
	<body>
		<slot />
	</body>
</html>
